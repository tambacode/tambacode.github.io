html
    title GoFarmer - Meus anúncios    
    include templates/head
    
    body
        include templates/sideBar
        include templates/filterSideBar
        .pusher
            include templates/header
            include templates/AnunciarAgora
            #all
                br
                #ads.sharedAds.ui.grid
                    include templates/loader
                br
                br
            include templates/footer
        script.
            if (navigator.onLine) {
                pageReadyDesired = 0;

                sw_Register();

                ad_List_ListAdsByUser("mylist", false);
            } else {
                sw_SetLastPageState();
            }